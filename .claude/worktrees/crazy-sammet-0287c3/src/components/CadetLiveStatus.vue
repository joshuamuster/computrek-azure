<!--
  CadetLiveStatus.vue — DEPRECATED (Plan Betazoid)

  This component has been split into three focused components
  now mounted directly in TopNav.vue:

    src/components/hud/TypingReport.vue    ← left panel
    src/components/hud/HudShip.vue         ← center ship
    src/components/hud/MissionReports.vue  ← right panel

  Shared bar logic lives in:
    src/composables/useHudMetrics.js

  Shared panel CSS lives in:
    src/assets/styles/hud.css

  This file is safe to delete once you've confirmed nothing else imports it.
-->
